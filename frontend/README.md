# Frontend (Vite + React + TS + Tailwind)

## Install & run

```bash
cd frontend
npm install
npm run dev
```

Открывается на http://localhost:5173

Бек должен работать на http://localhost:8000 (см. `../backend/README.md`).
